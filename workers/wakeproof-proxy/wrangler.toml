# ARCHIVED — DO NOT DEPLOY. See ./README.md.
#
# The worker name is deliberately renamed and the custom-domain route is commented
# out so `wrangler deploy` from this directory cannot accept production traffic.
# The live `wakeproof.aspiratcm.com` deployment from an earlier build still exists;
# delete it from the Cloudflare dashboard (Workers & Pages → wakeproof-proxy → Delete)
# to fully retire the archived code path. Production traffic now flows through
# ../wakeproof-proxy-vercel/.
name = "wakeproof-proxy-archived"
main = "worker.js"
compatibility_date = "2026-04-23"

# routes = [
#   { pattern = "wakeproof.aspiratcm.com", custom_domain = true }
# ]
# [placement]
# mode = "smart"
